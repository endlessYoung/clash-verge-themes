# Spec 15.1 — Depth pass (still skill-bound)

Critique of 5d12b4f: tokens correct, but surfaces read as empty skins — too little hierarchy, type ladder, and instrument chrome.

## Enrichment (keep skill bans)

**Cyber Nexus signature:** neural mesh field + cyan *signal filament* on active modules + live telemetry beacon. Still no chamfer / magenta / CRT / Orbitron.

**AI Operator signature:** Linear elevation stack + blue *tool-strip* on cards + denser type ladder + approval-grade focus rings. Still no phosphor / `$` / neon bloom.

## Depth checklist

- Layered sidebar / header / card elevations
- Eyebrow labels + stronger title weights
- Traffic as hero instrument (per-theme voice)
- 2–3 intentional motions each
- Hover/focus hierarchy louder than default
